# NASA CDF samples

`i2_av_bur_1973077231124_v01.cdf` is paired with
`data/raw/csa_verified_bur_1973077231124.png`. It lets notebook 03 compare a
generated model CDF's structure and shared variables with a NASA CDF.

The other CDFs are reference-only development samples. Model output is a
prediction, so matching file structure does not imply matching amplitudes.
