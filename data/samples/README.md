# NASA CDF samples

`i2_av_ksh_1972322002235_v01.cdf` is the reference paired with
`data/raw/csa_verified_ksh_1972322002235.png`. It is included so notebook 03
can compare the generated model CDF's structure and shared variables with a
known NASA CDF.

The other CDFs are reference-only development samples. The model output is a
prediction, so matching file structure does not imply matching amplitudes.
